---

docker:
  create_containers: 1
  default_container_name: mdbook
  default_container_image: hrektts/mdbook
  default_container_command: sleep 1d
  DOCKER_PACKAGES:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg-agent
    - software-properties-common
